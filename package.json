{
  "name": "rain-local-db-sync",
  "version": "0.1.0",
  "description": "Utilities for syncing Rain orderbook data into a local SQLite database.",
  "scripts": {
    "build": "tsc",
    "sync": "ts-node src/sync.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
